# pylydice

A polyhedral die rolling tool and python module.

> WARNING: this is just a python-learning project, not a real application or library.

## Development

This project uses "poetry":

    poetry install
    poetry shell
    pylyd -h

The "black" formatter and "pylint" tools are also available vai:

    poetry run <tool>

